a<-5
b<-6
c<-a*b**3-b/a
print(c)

# Que fait l'oporteur ** ?
# ** est l'opérateur de puissance. Il permet de calculer une puissance.

5->a
6->b
c<-a*b+b**3-b/a
print(c)

# Obtient-on le même résultat que le script précédent ?
# Non, on n'obtient pas le même résultat.


a=5
b=6
c=a*b**3-b/a
print(c)

# Obtient-on le même résultat que le script précédent ?
# Non, on n'obtient pas le même résultat que le script précédent mais on obtient le même résultat que le script exo18-1.r.